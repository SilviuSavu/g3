# CI/CD configuration using shared template
include:
  - project: 'root/ci-template'
    ref: main
    file: '.gitlab-ci-ai-review.yml'

# Override rules for ALL jobs
workflow:
  rules:
    - when: always
